{% for interface in sg_network %}
{% if interface.services is defined %}
{% if "ntp" in interface.services %}
listen on {{ interface.address }}
{% endif %}
{% endif %}
{% endfor %}

{% for clock in sg_globalconf.ntp.clocks %}
server {{ clock }}
{% endfor %}
